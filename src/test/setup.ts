/**
 * @file src/test/setup.ts
 * @description Global test setup file for Vitest.
 * Imports @testing-library/jest-dom to extend Vitest's expect
 * with DOM-specific matchers like toBeInTheDocument(), toHaveAttribute(), etc.
 */

import "@testing-library/jest-dom";
